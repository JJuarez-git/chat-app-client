import React from "react";
import Chat from "./Chat";

const ChatRoot = (props: any) => {
  return (
    <section className="w-full bg-gray-200 dark:bg-neutral-800">
      <Chat />
    </section>
  );
};

export default ChatRoot;
